interface:
  display_name: "NumOJ Admin"
  short_description: "NumOJ 管理员与可见用户功能 CLI"
  default_prompt: "Use $numoj-admin to manage NumOJ administrator and visible user workflows through the bundled CLI."
policy:
  allow_implicit_invocation: true
