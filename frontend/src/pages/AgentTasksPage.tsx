import {useMutation, useQuery, useQueryClient} from '@tanstack/react-query'
import {useEffect, useMemo, useState, type ChangeEvent, type FormEvent} from 'react'
import {createPortal} from 'react-dom'
import {useSearchParams} from 'react-router-dom'

import {apiFetch, errorMessage, queryString} from '../api/client'
import type {ApiEnvelope, JsonRecord} from '../api/types'
import {Identicon} from '../components/Identicon'
import {ModelLogo} from '../components/ModelLogo'
import {Link, useNavigate} from '../components/PageNavigation'
import {ErrorState, LoadingState} from '../components/PageState'

interface Response extends ApiEnvelope {
  user?: {is_admin?: number}
  agent_sessions: JsonRecord[]
  harnesses: JsonRecord[] | string[]
  endpoints_by_harness: Record<string, JsonRecord[]>
  reasoning_efforts_by_harness: Record<string, Array<JsonRecord | string>>
  preference?: {harness?: string; endpoint_id?: string | number; access_role?: string}
  agent_quota_summary: JsonRecord
  agent_personal_endpoints?: JsonRecord[]
  agent_quota_pending_count?: number
  agent_quota_pending_requests?: JsonRecord[]
  agent_scope?: string
  current_page?: number
  total_pages?: number
  page_numbers?: Array<number | null>
}

interface AgentAdminAccessResponse extends ApiEnvelope {
  requests?: JsonRecord[]
  classes?: JsonRecord[]
}

interface AgentEndpointListResponse extends ApiEnvelope {endpoints?: JsonRecord[]}
interface AgentEndpointTestResponse extends ApiEnvelope {test_token?: string; test?: JsonRecord}

type ChoiceOption = {value: string; label: string; icon?: string; model?: string}

function harnessIcon(value: string) {
  const normalized = value.toLowerCase().replaceAll('_', '-')
  const key = ['codex', 'opencode', 'pi'].includes(normalized) ? normalized : 'claude-code'
  return `harness-logo harness-logo--${key}`
}

function harnessLabel(value: unknown) {
  const text = String(value || 'Harness')
  return ({claude_code: 'Claude Code', 'claude-code': 'Claude Code', codex: 'Codex', opencode: 'OpenCode', pi: 'Pi'} as Record<string, string>)[text] || text
}

function AgentClassLogo({item, className}: {item?: JsonRecord; className: string}) {
  const cells = Array.isArray(item?.logo) ? item.logo : Array.isArray((item?.logo as JsonRecord | undefined)?.cells) ? (item?.logo as JsonRecord).cells as unknown[] : []
  if (!cells.length) return <span className={`${className} is-placeholder`} aria-hidden="true" />
  return <span className={className} aria-hidden="true"><svg viewBox="0 0 7 7" focusable="false" shapeRendering="crispEdges">{cells.map((cell, index) => Array.isArray(cell) && cell.length >= 2 ? <rect key={index} x={Number(cell[0]) + 1} y={Number(cell[1]) + 1} width="1" height="1" /> : null)}</svg></span>
}

function Choice({value, label, icon, model, options, onChange}: {value: string; label: string; icon: string; model?: string; options: ChoiceOption[]; onChange: (value: string) => void}) {
  const [open, setOpen] = useState(false)
  const selected = options.find((option) => option.value === value)
  const iconNode = (className = icon, modelName?: string) => modelName ? <ModelLogo model={modelName} /> : <i className={className.startsWith('fa-') ? `fas ${className}` : className} />
  return <div className={`rk-choice agent-choice${open ? ' open' : ''}`}>
    <input className="rk-choice-value" value={value} readOnly tabIndex={-1} aria-hidden="true" />
    <button type="button" className="rk-choice-trigger" role="combobox" aria-haspopup="listbox" aria-expanded={open} onClick={() => setOpen((current) => !current)}><span className="rk-choice-trigger-main">{iconNode(selected?.icon || icon, selected?.model || ((selected?.icon || icon).includes('fa-microchip') ? selected?.label || label : model))}<span>{label}</span></span><i className="fas fa-chevron-down rk-choice-caret" /></button>
    <div className="rk-choice-menu" role="listbox">{options.map((option) => <button type="button" className={`rk-choice-option${option.value === value ? ' active' : ''}`} role="option" aria-selected={option.value === value} onClick={() => {onChange(option.value); setOpen(false)}} key={option.value}><span className="rk-choice-option-main">{iconNode(option.icon || icon, option.model || ((option.icon || icon).includes('fa-microchip') ? option.label : undefined))}<span><span className="rk-choice-option-name">{option.label}</span></span></span><i className="fas fa-check rk-choice-option-check" /></button>)}</div>
  </div>
}

function PersonalEndpointLayer({endpoint, close, refresh}: {endpoint: JsonRecord | null; close: () => void; refresh: () => Promise<unknown>}) {
  const [form, setForm] = useState<Record<string, string>>(() => ({
    protocol: String(endpoint?.protocol || 'openai'), category: String(endpoint?.category || 'text'), base_url: String(endpoint?.base_url || ''), api_key: '', model: String(endpoint?.model || ''), context_window_tokens: String(endpoint?.context_window_tokens || 384000), max_output_tokens: String(endpoint?.max_output_tokens || 32000),
  }))
  const [thinking, setThinking] = useState(Boolean(endpoint?.thinking_enabled))
  const [testToken, setTestToken] = useState('')
  const [openChoice, setOpenChoice] = useState('')
  const payload = () => ({...form, name: form.model, thinking_enabled: thinking && form.category !== 'embedding', thinking_format: thinking && form.category !== 'embedding' ? (form.protocol === 'anthropic' ? 'thinking_type' : 'enable_thinking') : 'none', endpoint_id: endpoint?.id})
  const testEndpoint = useMutation({mutationFn: () => apiFetch<AgentEndpointTestResponse>('/api/agent/endpoints/test', {method: 'POST', body: JSON.stringify(payload())}), onSuccess: (data) => {setTestToken(String(data.test_token || '')); const tested = data.test || {}; if (tested.context_window_tokens || tested.max_output_tokens) setForm((current) => ({...current, context_window_tokens: String(tested.context_window_tokens || current.context_window_tokens), max_output_tokens: String(tested.max_output_tokens || current.max_output_tokens)}))}})
  const saveEndpoint = useMutation({mutationFn: () => apiFetch<ApiEnvelope>(endpoint ? `/api/agent/endpoints/${String(endpoint.id)}` : '/api/agent/endpoints', {method: endpoint ? 'PUT' : 'POST', body: JSON.stringify({...payload(), test_token: testToken})}), onSuccess: async () => {await refresh(); close()}})
  const set = (key: string, next: string) => {setTestToken(''); setForm((current) => ({...current, [key]: next}))}
  const choice = (key: 'protocol' | 'category', options: ChoiceOption[]) => {const selected = options.find((item) => item.value === form[key]) || options[0]; return <div className={`rk-choice numoj-endpoint-editor__choice${openChoice === key ? ' open' : ''}`}><button className="rk-choice-trigger" type="button" role="combobox" aria-haspopup="listbox" aria-expanded={openChoice === key} onClick={() => setOpenChoice((current) => current === key ? '' : key)}><span className="rk-choice-trigger-main"><i className={`fas ${selected.icon}`} /><span>{selected.label}</span></span><i className="fas fa-chevron-down rk-choice-caret" /></button>{openChoice === key ? <div className="rk-choice-menu" role="listbox">{options.map((item) => <button type="button" className={`rk-choice-option${item.value === form[key] ? ' active' : ''}`} role="option" aria-selected={item.value === form[key]} key={item.value} onClick={() => {set(key, item.value); if (key === 'category' && item.value === 'embedding') setThinking(false); setOpenChoice('')}}><span className="rk-choice-option-main"><i className={`fas ${item.icon}`} /><span><span className="rk-choice-option-name">{item.label}</span></span></span><i className="fas fa-check rk-choice-option-check" /></button>)}</div> : null}</div>}
  const statusError = testEndpoint.error || saveEndpoint.error
  return <section className="agent-access-layer agent-access-layer--endpoint" role="dialog" aria-modal="true" aria-labelledby="agentPersonalEndpoint-title"><button className="agent-access-layer-scrim" type="button" tabIndex={-1} aria-label="关闭端点编辑器" onClick={close} /><form className="numoj-endpoint-editor numoj-endpoint-editor--layer" autoComplete="off" noValidate onSubmit={(event) => {event.preventDefault(); if (testToken) saveEndpoint.mutate()}}><header className="numoj-endpoint-editor__header"><div><p>ENDPOINT EDITOR</p><h2 id="agentPersonalEndpoint-title" tabIndex={-1}>{endpoint ? `编辑端点 #${String(endpoint.id)}` : '新建端点'}</h2></div><button type="button" className="btn-close" aria-label="关闭" onClick={close} /></header><div className="numoj-endpoint-editor__body"><label className="wide"><span>模型名称</span><input value={form.model} onChange={(event) => set('model', event.target.value)} maxLength={200} required autoComplete="off" /></label><label><span>上下文窗口</span><input value={form.context_window_tokens} onChange={(event) => set('context_window_tokens', event.target.value)} type="number" min={1} max={2147483647} step={1} inputMode="numeric" required /></label><label><span>单次最大输出</span><input value={form.max_output_tokens} onChange={(event) => set('max_output_tokens', event.target.value)} type="number" min={1} max={2147483647} step={1} inputMode="numeric" required /></label><label><span>兼容协议</span>{choice('protocol', [{value: 'openai', label: 'OpenAI 兼容', icon: 'fa-code'}, {value: 'anthropic', label: 'Anthropic 兼容', icon: 'fa-brain'}])}</label><label><span>能力类别</span>{choice('category', [{value: 'text', label: '纯文本', icon: 'fa-font'}, {value: 'omni', label: '全模态', icon: 'fa-layer-group'}, {value: 'vision', label: '视觉理解', icon: 'fa-eye'}, {value: 'embedding', label: 'Embedding', icon: 'fa-vector-square'}])}</label><label className="wide"><span>基础地址</span><input value={form.base_url} onChange={(event) => set('base_url', event.target.value)} type="url" required autoComplete="url" placeholder="https://api.example.com/v1" /></label><label><span>API 密钥</span><input value={form.api_key} onChange={(event) => set('api_key', event.target.value)} type="password" required={!endpoint?.api_key_configured} autoComplete="new-password" placeholder="sk-..." /><small>{endpoint?.api_key_configured ? '留空表示继续使用已保存的密钥。' : '新建端点必须填写 API 密钥。'}</small></label>{form.category !== 'embedding' ? <label><span>思考模式</span><button className="numoj-endpoint-editor__thinking" type="button" role="switch" aria-checked={thinking} onClick={() => {setTestToken(''); setThinking((current) => !current)}}><i /><b>{thinking ? '开启' : '关闭'}</b></button></label> : null}{testEndpoint.isPending || testEndpoint.isSuccess || statusError ? <p className={`numoj-endpoint-editor__result wide${testEndpoint.isPending ? ' is-pending' : testEndpoint.isSuccess ? ' is-ok' : ' is-error'}`} role="status">{testEndpoint.isPending ? '测试中…' : testEndpoint.isSuccess ? `连接成功${testEndpoint.data.test?.latency_ms != null ? ` · ${String(testEndpoint.data.test.latency_ms)} ms` : ''}${testEndpoint.data.test?.message ? ` · ${String(testEndpoint.data.test.message)}` : ''}` : errorMessage(statusError)}</p> : null}</div><footer className="numoj-endpoint-editor__footer"><button className="numoj-endpoint-editor__secondary" type="button" onClick={close}>放弃</button><button className="numoj-endpoint-editor__secondary" type="button" disabled={testEndpoint.isPending || saveEndpoint.isPending} onClick={() => testEndpoint.mutate()}><i className="fas fa-vial" />{testEndpoint.isPending ? '测试中…' : '测试'}</button><button className="numoj-endpoint-editor__primary" type="submit" disabled={!testToken || saveEndpoint.isPending}>{saveEndpoint.isPending ? '保存中…' : '保存'}</button></footer></form></section>
}

function AgentAccessControl({data, isAdmin}: {data: Response; isAdmin: boolean}) {
  const queryClient = useQueryClient()
  const [open, setOpen] = useState(false)
  const [tab, setTab] = useState(isAdmin ? 'reviews' : 'quota')
  const [reason, setReason] = useState('')
  const [reviewAmount, setReviewAmount] = useState<Record<string, string>>({})
  const [reviewNote, setReviewNote] = useState<Record<string, string>>({})
  const [selectedClasses, setSelectedClasses] = useState<string[]>([])
  const [grantAmount, setGrantAmount] = useState('')
  const [classPickerOpen, setClassPickerOpen] = useState(false)
  const [classSearch, setClassSearch] = useState('')
  const [endpointEditor, setEndpointEditor] = useState<JsonRecord | null | undefined>(undefined)
  const [deleteEndpoint, setDeleteEndpoint] = useState<JsonRecord | null>(null)
  useEffect(() => {
    if (!open) return undefined
    const closeOnEscape = (event: KeyboardEvent) => {
      if (event.key === 'Escape' && endpointEditor === undefined && !deleteEndpoint) setOpen(false)
    }
    document.body.classList.add('modal-open')
    document.addEventListener('keydown', closeOnEscape)
    return () => {
      document.body.classList.remove('modal-open')
      document.removeEventListener('keydown', closeOnEscape)
    }
  }, [deleteEndpoint, endpointEditor, open])
  const adminAccess = useQuery({queryKey: ['agent-access', 'admin'], queryFn: () => apiFetch<AgentAdminAccessResponse>('/api/agent/quota/requests/pending'), enabled: open && isAdmin})
  const personalEndpoints = useQuery({queryKey: ['agent-access', 'personal-endpoints'], queryFn: () => apiFetch<AgentEndpointListResponse>('/api/agent/endpoints'), enabled: open})
  const prices = useQuery({queryKey: ['agent-access', 'prices'], queryFn: () => apiFetch<AgentEndpointListResponse>('/api/agent/endpoints/prices'), enabled: open && !isAdmin})
  const pending = adminAccess.data?.requests || data.agent_quota_pending_requests || []
  const quotaClasses = adminAccess.data?.classes || []
  const summary = data.agent_quota_summary || {}
  const requestQuota = useMutation({mutationFn: () => apiFetch<ApiEnvelope>('/api/agent/quota/requests', {method: 'POST', body: JSON.stringify({reason})}), onSuccess: async () => {setReason(''); await queryClient.invalidateQueries({queryKey: ['agent-tasks']})}})
  const review = useMutation({mutationFn: ({id, action}: {id: number; action: 'approve' | 'reject'}) => apiFetch<ApiEnvelope>(`/api/agent/quota/requests/${id}/review`, {method: 'POST', body: JSON.stringify({action, approved_amount: reviewAmount[String(id)] || undefined, review_note: reviewNote[String(id)] || ''})}), onSuccess: async () => {await Promise.all([queryClient.invalidateQueries({queryKey: ['agent-tasks']}), queryClient.invalidateQueries({queryKey: ['agent-access', 'admin']})])}})
  const grant = useMutation({mutationFn: () => apiFetch<ApiEnvelope>('/api/agent/quota/grants/class-batch', {method: 'POST', body: JSON.stringify({classes: selectedClasses, amount_rmb: grantAmount})}), onSuccess: async () => {setSelectedClasses([]); setGrantAmount(''); await Promise.all([queryClient.invalidateQueries({queryKey: ['agent-tasks']}), queryClient.invalidateQueries({queryKey: ['agent-access', 'admin']})])}})
  const removeEndpoint = useMutation({mutationFn: (id: number) => apiFetch<ApiEnvelope>(`/api/agent/endpoints/${id}`, {method: 'DELETE'}), onSuccess: async () => {setDeleteEndpoint(null); await Promise.all([queryClient.invalidateQueries({queryKey: ['agent-access', 'personal-endpoints']}), queryClient.invalidateQueries({queryKey: ['agent-tasks']})])}})
  const refreshEndpoints = () => Promise.all([queryClient.invalidateQueries({queryKey: ['agent-access', 'personal-endpoints']}), queryClient.invalidateQueries({queryKey: ['agent-tasks']})])
  const endpointItems = personalEndpoints.data?.endpoints || data.agent_personal_endpoints || []
  const selectedClassItems = quotaClasses.filter((item) => selectedClasses.includes(String(item.class_en || item.id || '')))
  const selectedUserCount = new Set(selectedClassItems.flatMap((item) => Array.isArray(item.user_ids) ? item.user_ids.map(String) : [])).size || selectedClassItems.reduce((sum, item) => sum + Number(item.user_count || item.student_count || 0), 0)
  const grantTotal = Number(grantAmount || 0) * selectedUserCount
  const tabs: Array<{value: string; label: string; icon?: string}> = isAdmin
    ? [{value: 'reviews', label: '申请审核'}, {value: 'grants', label: '批量赠送'}, {value: 'personal', label: '自定义端点'}]
    : [{value: 'quota', label: '额度与申请', icon: 'fa-wallet'}, {value: 'prices', label: '端点价格', icon: 'fa-tags'}, {value: 'personal', label: '自定义端点', icon: 'fa-key'}]
  return createPortal(<aside className="agent-access-control">
    <button className={`agent-access-fab${isAdmin ? ' agent-access-fab--review' : ''}`} type="button" aria-label={isAdmin ? '审核额度申请' : '查看 Agent 额度'} title={isAdmin ? '额度申请审核' : 'Agent 额度'} onClick={() => setOpen(true)}><i className={`fas ${isAdmin ? 'fa-clipboard-check' : 'fa-wallet'}`} />{isAdmin && Number(data.agent_quota_pending_count || 0) > 0 ? <span className="agent-access-fab-badge">{Number(data.agent_quota_pending_count)}</span> : null}</button>
    {open ? <><div className="modal fade show d-block agent-access-modal" role="dialog" aria-modal="true" aria-labelledby="agentAccessModalTitle"><div className="modal-dialog modal-dialog-centered modal-dialog-scrollable"><div className="modal-content">
      <header className="agent-access-modal-header"><div><span>{isAdmin ? 'REQUEST DESK' : 'AGENT WALLET'}</span><h2 id="agentAccessModalTitle">{isAdmin ? '额度申请审核' : '额度与端点'}</h2></div><button type="button" className="btn-close" aria-label="关闭" onClick={() => setOpen(false)} /></header>
      <div className="agent-access-modal-scroll"><nav className={isAdmin ? 'agent-access-admin-tabs' : 'agent-access-user-tabs'} role="tablist" aria-label={isAdmin ? '额度管理' : 'Agent 额度与端点'}>{tabs.map((item) => <button type="button" role="tab" className={tab === item.value ? 'is-current' : ''} aria-selected={tab === item.value} onClick={() => setTab(item.value)} key={item.value}>{item.icon ? <i className={`fas ${item.icon}`} /> : null}<span>{item.label}</span></button>)}</nav>
        {isAdmin && tab === 'reviews' ? <section role="tabpanel" aria-label="申请审核"><div className="agent-access-review-summary"><span>等待处理</span><strong><b>{pending.length}</b> 项</strong></div><div className="agent-access-review-list">{adminAccess.isPending ? <div className="agent-access-loading">读取申请中</div> : adminAccess.isError ? <div className="agent-access-empty">{adminAccess.error.message}</div> : pending.length ? pending.map((item) => {const id = String(item.id || item.request_id || ''); return <article className="agent-access-review-card" key={id}><header><div><span className="agent-personal-endpoint-number">额度申请 #{id}</span><h3>{String(item.username || item.user_name || `用户 #${String(item.user_id || '')}`)}</h3></div><div className="agent-access-review-meta"><time>{String(item.created_at || '')}</time></div></header>{item.class_name || item.class_label || item.class_en ? <span className="agent-access-review-class"><i className="fas fa-users" />{String(item.class_name || item.class_label || item.class_en)}</span> : null}<p className="agent-access-review-reason">{String(item.reason || '未填写申请理由')}</p><form className="agent-access-review-form" onSubmit={(event) => {event.preventDefault(); review.mutate({id: Number(id), action: 'approve'})}}><label className="agent-access-field"><span>赠送额度</span><span className="agent-access-input-shell agent-access-money-input"><b>¥</b><input type="number" min="0.01" step="0.01" inputMode="decimal" placeholder="0.00" aria-label="赠送额度" required value={reviewAmount[id] || ''} onChange={(event) => setReviewAmount((current) => ({...current, [id]: event.target.value}))} /></span></label><label className="agent-access-field"><span>审核意见</span><span className="agent-access-input-shell"><i className="fas fa-pen" /><input maxLength={1000} placeholder="可选" aria-label="审核意见" value={reviewNote[id] || ''} onChange={(event) => setReviewNote((current) => ({...current, [id]: event.target.value}))} /></span></label><div className="agent-access-review-actions"><button type="submit" disabled={!reviewAmount[id]}>通过申请</button><button type="button" onClick={() => review.mutate({id: Number(id), action: 'reject'})}>驳回</button></div></form></article>}) : <div className="agent-access-empty">没有等待处理的额度申请</div>}</div></section> : null}
        {isAdmin && tab === 'grants' ? <section className="agent-class-grant-panel" role="tabpanel" aria-label="批量赠送"><header><span>CLASS GRANT</span><h3>按班级赠送额度</h3></header><form onSubmit={(event) => {event.preventDefault(); if (selectedClasses.length && selectedUserCount && Number(grantAmount) > 0) grant.mutate()}} noValidate><fieldset className="agent-class-grant-fieldset"><legend>选择班级</legend><div className={`agent-class-picker${classPickerOpen ? ' is-open' : ''}`}><button type="button" className="agent-class-picker-trigger" aria-haspopup="listbox" aria-expanded={classPickerOpen} onClick={() => setClassPickerOpen((current) => !current)} disabled={!quotaClasses.length}><AgentClassLogo className="agent-class-picker-logo" item={selectedClassItems[0]} /><span className="agent-class-picker-current"><strong>{selectedClasses.length ? `已选择 ${selectedClasses.length} 个班级` : '选择班级'}</strong><small>{selectedClasses.length ? selectedClasses.join(' · ') : 'MULTIPLE SELECT'}</small></span><i className="fas fa-chevron-down agent-class-picker-caret" /></button>{classPickerOpen ? <div className="agent-class-picker-panel" role="listbox" aria-label="选择班级" aria-multiselectable="true"><label className="agent-class-picker-search agent-access-input-shell"><i className="fas fa-magnifying-glass" /><input type="search" placeholder="搜索班级" autoComplete="off" spellCheck={false} aria-label="搜索班级" value={classSearch} onChange={(event) => setClassSearch(event.target.value)} /></label><div className="agent-class-grant-options">{quotaClasses.filter((item) => `${String(item.label || item.class_en)} ${String(item.class_en)}`.toLowerCase().includes(classSearch.trim().toLowerCase())).map((item) => {const key = String(item.class_en || item.id || ''); return <label className="agent-class-option" key={key}><input className="agent-class-checkbox" type="checkbox" name="classes" value={key} checked={selectedClasses.includes(key)} onChange={(event) => setSelectedClasses((current) => event.target.checked ? [...current, key] : current.filter((value) => value !== key))} /><AgentClassLogo className="agent-class-option-logo" item={item} /><span className="agent-class-option-copy"><strong>{String(item.label || item.class_en || '未命名班级')}</strong><small>{key}</small></span><span className="agent-class-option-state" aria-hidden="true"><i className="fas fa-check" /></span></label>})}</div><div className="agent-class-picker-empty" hidden={Boolean(quotaClasses.length)}>无匹配班级</div><footer className="agent-class-picker-footer"><button type="button" onClick={() => setSelectedClasses(quotaClasses.map((item) => String(item.class_en || item.id || '')))}>全选</button><button type="button" onClick={() => setSelectedClasses([])}>清空</button><button type="button" className="is-done" onClick={() => setClassPickerOpen(false)}>完成</button></footer></div> : null}</div></fieldset><label className="agent-class-grant-amount agent-access-field"><span>每人赠送</span><span className="agent-access-input-shell agent-access-money-input"><b>¥</b><input name="amount_rmb" type="number" min="0.01" step="0.01" inputMode="decimal" required placeholder="0.00" value={grantAmount} onChange={(event) => setGrantAmount(event.target.value)} /></span></label><dl className="agent-class-grant-preview" aria-live="polite"><div><dt>去重用户</dt><dd><b>{selectedUserCount}</b> 人</dd></div><div><dt>赠送总额</dt><dd>{Number.isFinite(grantTotal) ? `${grantTotal} 元` : '—'}</dd></div></dl><button type="submit" disabled={!selectedClasses.length || !selectedUserCount || !grantAmount || Number(grantAmount) <= 0 || grant.isPending}>确认赠送</button></form>{grant.isError ? <p className="agent-access-feedback is-error">{errorMessage(grant.error)}</p> : null}</section> : null}
        {!isAdmin && tab === 'quota' ? <section>{summary.public_enabled === false ? <div className="agent-access-public-notice"><i className="fas fa-pause-circle" /><div><strong>Agent 暂停向普通用户开放</strong><span>已有会话仍可查看，但不能申请额度、创建任务或继续对话。</span></div></div> : null}<section className="agent-access-balance" aria-labelledby="agentBalanceTitle"><header><span>ACCOUNT</span><h3 id="agentBalanceTitle">额度概览</h3></header><dl><div><dt>已用额度</dt><dd>¥ {String(summary.used_amount ?? '0')}</dd></div><div className="is-remaining"><dt>剩余额度</dt><dd>¥ {String(summary.remaining_amount ?? '0')}</dd></div></dl></section><section className="agent-access-section"><header><div><span>REQUEST</span><h3>申请额度</h3></div></header><form className="agent-quota-request-form" onSubmit={(event) => {event.preventDefault(); requestQuota.mutate()}}><label className="agent-access-field agent-quota-request-reason"><span>申请理由</span><span className="agent-access-input-shell is-textarea"><textarea name="reason" rows={3} maxLength={1000} required placeholder="简要说明你准备让 Agent 完成什么任务" value={reason} onChange={(event) => setReason(event.target.value)} /></span></label><button className="agent-access-primary agent-quota-request-submit" type="submit" disabled={!reason.trim() || requestQuota.isPending}><span>提交申请</span><i className="fas fa-arrow-right" /></button></form>{requestQuota.isError ? <p className="agent-access-feedback">{errorMessage(requestQuota.error)}</p> : null}</section></section> : null}
        {tab === 'prices' ? <section role="tabpanel" aria-label="端点价格"><div className="agent-access-panel-intro"><div><span>RATE CARD</span><h3>全站端点价格</h3></div><p>使用全站端点时按实际 Token 实时结算，以下价格单位均为元 / 1M Token。</p></div><div className="agent-rate-list">{prices.isPending ? <div className="agent-access-loading">读取价格中</div> : prices.isError ? <div className="agent-access-empty">{prices.error.message}</div> : (prices.data?.endpoints || []).length ? (prices.data?.endpoints || []).map((item) => <article className="agent-rate-card" key={String(item.id)}><header className="agent-rate-card-header"><span className="agent-rate-logo"><ModelLogo model={item.model} /></span><div className="agent-rate-card-name"><strong title={String(item.model || '')}>{String(item.model || '未命名模型')}</strong><small>{String(item.protocol || 'openai') === 'anthropic' ? 'Anthropic 兼容' : 'OpenAI 兼容'}</small></div></header><dl className="agent-rate-values" aria-label="节点价格，人民币每百万 Token"><div><dt>INPUT</dt><dd>{String(item.input_price_per_million || '—')}</dd></div><div><dt>CACHED</dt><dd>{String(item.cached_input_price_per_million || '—')}</dd></div><div><dt>OUTPUT</dt><dd>{String(item.output_price_per_million || '—')}</dd></div></dl></article>) : <div className="agent-access-empty">暂无可用的全站端点</div>}</div></section> : null}
        {tab === 'personal' ? <section className="agent-personal-endpoints" role="tabpanel" aria-label="自定义端点"><header className="agent-personal-endpoints-heading"><div><span>BRING YOUR OWN KEY</span><h3>自定义端点</h3><p>自有端点使用你的 API 密钥，不消耗平台额度。</p></div><button className="agent-access-secondary is-strong" type="button" onClick={() => setEndpointEditor(null)}><i className="fas fa-plus" />新建端点</button></header><div className="agent-personal-endpoint-list">{personalEndpoints.isPending ? <div className="agent-access-loading">读取端点中</div> : personalEndpoints.isError ? <div className="agent-access-empty">{personalEndpoints.error.message}</div> : endpointItems.length ? endpointItems.map((item) => {const id = String(item.id || item.endpoint_id || ''); const name = String(item.name || item.label || item.model || '自定义端点'); return <article className="agent-personal-endpoint-card" key={id}><div className="agent-personal-endpoint-main"><div className="agent-personal-endpoint-top"><div><span className="agent-personal-endpoint-number">自有节点 #{id}</span><h3 className="agent-personal-endpoint-title"><ModelLogo model={item.model || name} /><span title={name}>{name}</span></h3></div><span className="agent-personal-endpoint-chip">{String(item.protocol || 'openai') === 'anthropic' ? 'Anthropic 兼容' : 'OpenAI 兼容'}</span></div><div className="agent-personal-endpoint-url"><small>模型 · {String(item.model || '未命名')}</small><span title={String(item.base_url || '')}>{String(item.base_url || '未配置地址')}</span></div></div><footer className="agent-personal-endpoint-foot"><span className="agent-personal-endpoint-state"><i />{item.api_key_configured === false ? '密钥未配置' : '密钥已配置'}</span><button type="button" title="编辑" aria-label={`编辑 ${name}`} onClick={() => setEndpointEditor(item)}><i className="fas fa-pen" /></button><button className="is-danger" type="button" title="删除" aria-label={`删除 ${name}`} onClick={() => setDeleteEndpoint(item)}><i className="fas fa-trash-alt" /></button></footer></article>}) : null}</div>{removeEndpoint.isError ? <p className="agent-access-feedback is-error">{errorMessage(removeEndpoint.error)}</p> : null}</section> : null}
        {review.isError ? <p className="agent-access-feedback">{errorMessage(review.error)}</p> : null}
      </div>
      {endpointEditor !== undefined ? <PersonalEndpointLayer endpoint={endpointEditor} close={() => setEndpointEditor(undefined)} refresh={refreshEndpoints} /> : null}
      {deleteEndpoint ? <section className="agent-access-layer is-compact" role="alertdialog" aria-modal="true" aria-labelledby="agentPersonalEndpointDeleteTitle"><button className="agent-access-layer-scrim" type="button" tabIndex={-1} aria-label="取消删除" onClick={() => setDeleteEndpoint(null)} /><div className="agent-access-layer-panel"><div className="agent-endpoint-delete-body"><span className="agent-endpoint-delete-icon"><i className="fas fa-trash-alt" /></span><div><span>DELETE ENDPOINT</span><h2 id="agentPersonalEndpointDeleteTitle" tabIndex={-1}>删除自定义端点？</h2></div><p>将删除 <strong>{String(deleteEndpoint.name || deleteEndpoint.model || '这个端点')}</strong>。已有会话仍可查看，但不能再通过它继续运行。</p>{removeEndpoint.isError ? <p className="agent-access-feedback is-error">{errorMessage(removeEndpoint.error)}</p> : null}</div><footer className="agent-endpoint-delete-footer"><button className="agent-access-secondary" type="button" onClick={() => setDeleteEndpoint(null)}>保留端点</button><button className="agent-access-danger-button" type="button" disabled={removeEndpoint.isPending} onClick={() => removeEndpoint.mutate(Number(deleteEndpoint.id))}>确认删除</button></footer></div></section> : null}
    </div></div></div><div className="modal-backdrop fade show" onClick={() => setOpen(false)} /></> : null}
  </aside>, document.body)
}

export default function AgentTasksPage() {
  const queryClient = useQueryClient()
  const navigate = useNavigate()
  const [searchParams] = useSearchParams()
  const page = Math.max(1, Number(searchParams.get('page') || 1))
  const requestedScope = searchParams.get('scope') || ''
  const result = useQuery({queryKey: ['agent-tasks', page, requestedScope], queryFn: () => apiFetch<Response>(`/api/agent/sessions${queryString({page, scope: requestedScope || undefined})}`)})
  const [message, setMessage] = useState(() => searchParams.get('problem_id') ? `${searchParams.get('mode') === 'testdata' ? '为题目生成测试数据' : '求解题目'} #${searchParams.get('problem_id')}` : '')
  const [harness, setHarness] = useState('')
  const [endpointId, setEndpointId] = useState('')
  const [reasoningEffort, setReasoningEffort] = useState('')
  const [accessRole, setAccessRole] = useState('')
  const [attachments, setAttachments] = useState<File[]>([])
  const harnesses = useMemo(() => (result.data?.harnesses || []).map((item) => {
    const value = typeof item === 'string' ? item : String(item.value || item.id || item.name || '')
    return {value, label: typeof item === 'string' ? item : String(item.label || item.name || item.value || ''), icon: harnessIcon(value)}
  }), [result.data])
  const selectedHarness = harness || String(result.data?.preference?.harness || harnesses[0]?.value || '')
  const endpoints = result.data?.endpoints_by_harness?.[selectedHarness] || []
  const endpointValue = (item: JsonRecord) => String(item.ref || item.id || '')
  const selectedEndpoint = endpointId || String(result.data?.preference?.endpoint_id || (endpoints[0] ? endpointValue(endpoints[0]) : ''))
  const effortOptions = (result.data?.reasoning_efforts_by_harness?.[selectedHarness] || []).map((item) => typeof item === 'string' ? {value: item, label: item} : {value: String(item.value || ''), label: String(item.label || item.value || '')})
  const selectedEffort = effortOptions.some((item) => item.value === reasoningEffort) ? reasoningEffort : String(effortOptions.find((item) => item.value === 'high')?.value || effortOptions[0]?.value || 'default')
  const isAdmin = Number(result.data?.user?.is_admin || 0) === 1
  const selectedRole = accessRole || String(result.data?.preference?.access_role || 'user')
  const create = useMutation({mutationFn: () => {const form = new FormData(); form.append('message', message); form.append('harness', selectedHarness); form.append('endpoint_id', selectedEndpoint); form.append('reasoning_effort', selectedEffort); form.append('access_role', isAdmin ? selectedRole : 'user'); form.append('message_id', crypto.randomUUID()); attachments.forEach((file) => form.append('attachments', file)); return apiFetch<ApiEnvelope & {session_id?: string}>('/api/agent/sessions', {method: 'POST', body: form})}, onSuccess: async (data) => {setMessage(''); setAttachments([]); await queryClient.invalidateQueries({queryKey: ['agent-tasks']}); if (data.session_id) navigate(`/agents/${encodeURIComponent(data.session_id)}`)}})
  if (result.isPending) return <LoadingState label="正在读取 Agent 会话" />
  if (result.isError) return <ErrorState message={result.error.message} />
  const sessions = result.data?.agent_sessions || []
  const scope = result.data?.agent_scope || (isAdmin ? 'all' : 'mine')
  const currentPage = Number(result.data?.current_page || page)
  const totalPages = Number(result.data?.total_pages || 1)
  const pages = result.data?.page_numbers || []
  const submit = (event: FormEvent) => {event.preventDefault(); if (selectedEndpoint) create.mutate()}
  const chooseAttachments = (event: ChangeEvent<HTMLInputElement>) => setAttachments(Array.from(event.target.files || []))
  return <><main className="agent-home" data-agent-home>
    <section className="agent-home-hero" aria-labelledby="agentHomeTitle"><div className="agent-home-heading"><span className="agent-home-kicker">NUMERICAL OJ · AGENT</span><h1 id="agentHomeTitle">今天想让 Agent 做什么？</h1></div><form className="agent-composer" onSubmit={submit} autoComplete="off"><label className="visually-hidden" htmlFor="agentMessage">任务内容</label><textarea id="agentMessage" name="message" rows={4} maxLength={100000} placeholder="让 Agent 分析问题、编写代码或整理文件…" value={message} onChange={(event) => setMessage(event.target.value)} required />{attachments.length ? <div className="agent-attachment-strip" aria-label="待上传附件">{attachments.map((file, index) => <span className="agent-attachment-chip" key={`${file.name}-${index}`} title={file.name}><i className="fas fa-paperclip" /><span className="agent-attachment-chip-copy"><strong>{file.name}</strong><small>{Math.max(1, Math.ceil(file.size / 1024))} KB</small></span><button className="agent-attachment-remove" type="button" aria-label={`移除 ${file.name}`} onClick={() => setAttachments((items) => items.filter((_, itemIndex) => itemIndex !== index))}><i className="fas fa-times" /></button></span>)}</div> : null}<div className="agent-composer-footer"><div className="agent-composer-controls" aria-label="任务运行设置"><div className="agent-composer-choice agent-composer-choice--harness"><Choice value={selectedHarness} label={harnesses.find((item) => item.value === selectedHarness)?.label || '选择 Harness'} icon={harnessIcon(selectedHarness)} options={harnesses} onChange={(value) => {setHarness(value); setEndpointId(''); setReasoningEffort('')}} /></div><div className="agent-composer-choice agent-composer-choice--endpoint"><Choice value={selectedEndpoint} label={String(endpoints.find((item) => endpointValue(item) === selectedEndpoint)?.model || (endpoints.length ? '选择模型节点' : ''))} icon="fa-microchip" options={endpoints.map((item) => ({value: endpointValue(item), label: String(item.model || item.label || item.name || '')}))} onChange={setEndpointId} /></div>{effortOptions.length ? <div className="agent-composer-choice agent-composer-choice--effort"><Choice value={selectedEffort} label={effortOptions.find((item) => item.value === selectedEffort)?.label || '选择思考深度'} icon="agent-effort-logo agent-effort-logo--choice" options={effortOptions} onChange={setReasoningEffort} /></div> : null}{isAdmin ? <div className="agent-composer-choice agent-composer-choice--role"><Choice value={selectedRole} label={selectedRole === 'admin' ? '以管理员身份' : '以用户身份'} icon={selectedRole === 'admin' ? 'fa-user-shield' : 'fa-user'} options={[{value: 'user', label: '以用户身份', icon: 'fa-user'}, {value: 'admin', label: '以管理员身份', icon: 'fa-user-shield'}]} onChange={setAccessRole} /></div> : null}</div><div className="agent-composer-actions"><label className="agent-icon-button" title="添加附件" aria-label="添加附件"><input className="visually-hidden" type="file" multiple onChange={chooseAttachments} /><i className="fas fa-plus" /></label><button className="agent-send-button" type="submit" disabled={!message.trim() || !selectedEndpoint || create.isPending} aria-label="创建 Agent 会话"><i className="fas fa-arrow-up" /></button></div></div>{!endpoints.length ? <p className="agent-composer-feedback is-error" role="status">该 Harness 暂无兼容的模型节点。</p> : null}{create.isError ? <p className="agent-composer-feedback is-error" role="alert">{errorMessage(create.error)}</p> : null}</form></section>
    <section className="agent-history" aria-labelledby="agentHistoryTitle"><header className="agent-history-heading"><div><span className="agent-history-kicker">RECENT WORK</span><h2 id="agentHistoryTitle">历史会话</h2></div>{isAdmin ? <nav className="agent-history-scope" aria-label="会话范围"><Link to="/agents?scope=all" className={scope !== 'mine' ? 'is-current' : ''} aria-current={scope !== 'mine' ? 'page' : undefined}>全站会话</Link><Link to="/agents?scope=mine" className={scope === 'mine' ? 'is-current' : ''} aria-current={scope === 'mine' ? 'page' : undefined}>我的会话</Link></nav> : <span>按最近更新排序</span>}</header><div className="agent-history-list">{sessions.map((session, index) => {const actor = String(session.requested_by || session.created_by || 'numericaloj'); const id = String(session.session_id || session.task_id || index); const status = String(session.status || '').toLowerCase(); const runtime = String(session.harness || ''); return <Link className="agent-history-row" to={`/agents/${encodeURIComponent(id)}`} key={id}><Identicon seed={actor} className="agent-history-avatar" /><span className="agent-history-main"><strong title={String(session.title || '未命名任务')}>{String(session.title || '未命名任务')}</strong><small><span>{actor}</span><time>{String(session.updated_at || session.created_at || '')}</time>{['running', 'pending'].includes(status) ? <span className="agent-history-live"><i />运行中</span> : null}</small></span><span className="agent-history-runtime agent-history-runtime--harness"><i className={harnessIcon(runtime)} /><span>{harnessLabel(session.harness_label || session.harness)}</span></span><span className="agent-history-runtime"><ModelLogo model={session.endpoint_model || session.model} /><span>{String(session.endpoint_model || session.model || '模型节点')}</span></span><i className="fas fa-chevron-right agent-history-arrow" /></Link>})}{!sessions.length ? <div className="agent-history-empty"><span className="agent-history-empty-mark"><i className="fas fa-terminal" /></span><strong>还没有会话</strong><p>从上面的输入框开始，Agent 的工作记录会留在这里。</p></div> : null}</div>{totalPages > 1 ? <nav className="agent-history-pagination" aria-label="历史会话分页">{currentPage > 1 ? <Link to={`/agents${queryString({page: currentPage - 1, scope})}`} aria-label="上一页"><i className="fas fa-arrow-left" /></Link> : null}{pages.map((pageNumber, index) => pageNumber ? <Link to={`/agents${queryString({page: pageNumber, scope})}`} className={pageNumber === currentPage ? 'is-current' : ''} aria-current={pageNumber === currentPage ? 'page' : undefined} key={pageNumber}>{pageNumber}</Link> : <span aria-hidden="true" key={`gap-${index}`}>…</span>)}{currentPage < totalPages ? <Link to={`/agents${queryString({page: currentPage + 1, scope})}`} aria-label="下一页"><i className="fas fa-arrow-right" /></Link> : null}</nav> : null}</section>
  </main><AgentAccessControl data={result.data} isAdmin={isAdmin} /></>
}
